import React from 'react';
import './Loading.css';

const Loading = () => {
    return (
        <div class="d-flex justify-content-center">
            <span>Loading...</span>
        </div>
    );
};

export default Loading;